# Shell Shortcuts

Useful aliases:

alias ll='ls -lah'
alias gs='git status'
alias capply='cd ~/projects/applypilot'
alias check='bash scripts/check.sh'

Add aliases to ~/.bashrc, then run:

source ~/.bashrc
