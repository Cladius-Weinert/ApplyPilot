# Instructions for Codex

Improve this project with small, safe, reviewable changes.

Priorities:
- Make the project easy to run on Termux Android.
- Keep dependencies lightweight.
- Improve README, setup steps, .env.example, and error handling.
- Add simple commands for install, run, test, and lint if missing.
- Do not expose API keys, tokens, passwords, or private credentials.
- Before major edits, inspect the repo and explain the plan.
- After edits, run available tests or basic smoke checks.

Default output:
1. Problems found
2. Files changed
3. How to run
4. What to improve next